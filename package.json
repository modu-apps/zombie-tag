{
  "name": "brains",
  "version": "1.0.0",
  "private": true,
  "description": "Brains - Zombie Tag multiplayer game",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "dev": "node build.js --watch --serve"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
